<div class="container">
 <div class="row">
  <div class="col-md-12">
   <h1>{{title}}</h1>
   <ul>
    {{#each physicians}}
    <li><a href="/physicians/{{this.url_path}}">{{this.id}}. Dr. {{this.first_name}} {{this.last_name}}</a>
     <br>{{this.specialty}}
    </li>
    {{/each}}
   </ul>
  </div>
 </div>
</div>
